# Frontend Environment Variables for ChatterBox
# - Replace 'your-render-backend' with your actual Render service name
# - Must use https:// for production (HTTPS pages cannot connect to HTTP WebSockets)
# - Must use http:// for local development
# Note:

# NEXT_PUBLIC_WS_URL=https://your-render-backend.onrender.com/ws
# Production (Render backend):

NEXT_PUBLIC_WS_URL=http://localhost:8080/ws
# Local development:
# WebSocket URL for backend connection

# Set these in Vercel environment variables for production
# Copy this to frontend/.env.local for local development

