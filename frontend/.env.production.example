# Production environment variables for ChatterBox Frontend
# Copy to .env.production.local or set in Vercel → Project Settings → Environment Variables

# Backend API URL (Railway)
NEXT_PUBLIC_API_URL=https://chatterbox-production-2a2c.up.railway.app

# WebSocket URL — must be https:// when frontend is on HTTPS (Vercel)
NEXT_PUBLIC_WS_URL=https://chatterbox-production-2a2c.up.railway.app/ws

