# Backend Environment Variables for ChatterBox
# Copy this file to your Render environment or use as reference

# ====== REQUIRED ENVIRONMENT VARIABLES ======

# MongoDB Connection String (MongoDB Atlas)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database-name
SPRING_DATA_MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xyz.mongodb.net/?appName=Cluster0

# Active Spring Profile (use 'production' on Render)
SPRING_PROFILES_ACTIVE=production

# ====== OPTIONAL ENVIRONMENT VARIABLES ======

# CORS Allowed Origins (comma-separated)
# Example: https://your-vercel-frontend.vercel.app,https://*.vercel.app
CORS_ALLOWED_ORIGINS=https://your-vercel-frontend.vercel.app

# Port (automatically set by Render, but can override)
PORT=8080

# ====== HOW TO SET ON RENDER ======
# 1. Go to your Render service dashboard
# 2. Click on "Environment" tab
# 3. Add each variable with its value
# 4. Save and deploy

